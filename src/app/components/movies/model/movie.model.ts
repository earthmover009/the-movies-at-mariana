export class Movie {

    constructor( public title: string, public released: string, public rated: string, 
        public runtime: string, public genre: string[], public rating: string, public poster: string ){}
}